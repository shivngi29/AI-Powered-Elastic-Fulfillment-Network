{
  "name": "elastic-fulfillment-api",
  "version": "0.1.0",
  "private": true,
  "description": "Business API for the Elastic Fulfillment Network college project",
  "type": "module",
  "engines": {
    "node": ">=22.9.0"
  },
  "scripts": {
    "dev": "node --watch --env-file-if-exists=.env src/index.js",
    "start": "node --env-file-if-exists=.env src/index.js"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "mongoose": "^9.10.2"
  }
}
